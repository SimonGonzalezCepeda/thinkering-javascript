// VARIABLES GLOBALS: LA PESTE: XOCX
$name = 'Sergi';
function fire(bool) {
    console.log("Hola " + $name);

    if (bool) {
        foo = bar;

        console.log(foo);
    } else {
        console.log(foo);
    }
}

fire(true);